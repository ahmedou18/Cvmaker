providers = ["php", "node"]

[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = [
        "composer install --ignore-platform-reqs --no-dev --optimize-autoloader",
            "npm install"
]

[phases.build]
cmds = ["npm run build"]